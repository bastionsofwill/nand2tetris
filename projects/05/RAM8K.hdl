CHIP RAM8K {
    IN in[16], load, address[13];
    OUT out[16];

    PARTS:
    // Put your code here:
        DMux(in = load, sel = address[12], a = r1, b = r2);
        RAM4K(in = in, load = r1, address = address[0..11], out = r4k1);
        RAM4K(in = in, load = r2, address = address[0..11], out = r4k2);
        Mux16(a = r4k1, b = r4k2, sel = address[12], out = out);
}